# Вклад

Для крупной работы сначала откройте issue. Модель поведения остаётся независимой от Canvas/DOM; все функции доступны пультом; яркие неподвижные элементы на OLED не добавляются.

```sh
npm test
node --check src/main.js
```
